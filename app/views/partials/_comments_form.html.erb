<%= form_for current_user.comments.build do |f| %>
  <%= f.text_area :content, size: "45x1", class: "form-control" %>
  <%= f.hidden_field :post_id, value: locals[:friend_post_id] %>

  <%= f.submit "Post Comment", class: "btn btn-primary" %>

<% end %>
